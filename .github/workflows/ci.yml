name: CI

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, labeled]
  workflow_dispatch:

jobs:
  # Mỗi PR: nhanh (~1 phút). Bắt lỗi type, lint, format, unit/component test, build, bundle size.
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: yarn
      - run: yarn install --frozen-lockfile
      - run: yarn validate
      - run: yarn test
      - run: yarn build
      - run: yarn size

  # E2E nặng (cài Chromium, ~3 phút) nên chỉ chạy khi merge vào main, PR gắn label `e2e`,
  # hoặc chạy tay. Luồng chính vẫn được bảo vệ trước khi lên môi trường.
  e2e:
    runs-on: ubuntu-latest
    needs: check
    if: >-
      github.event_name == 'push' ||
      github.event_name == 'workflow_dispatch' ||
      contains(github.event.pull_request.labels.*.name, 'e2e')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: yarn
      - run: yarn install --frozen-lockfile
      - run: npx playwright install --with-deps chromium
      - run: yarn test:e2e
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report
          retention-days: 7
